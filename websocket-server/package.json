{
  "name": "websocket-server",
  "version": "1.0.0",
  "description": "WebSocket server to bridge WebSocket clients to TCP server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.12.0"
  }
}
